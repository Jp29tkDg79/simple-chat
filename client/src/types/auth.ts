export type LoginTypes = {
  email: string;
  password: string;
}

export type SigninTypes = {
  name: string;
  email: string;
  password: string;
}

export type RequestSuccessTypes = {
  name: string;
  email: string;
  token: string;
};